{
  "name": "goose-teams-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Teams bot ACP client for the Goose Agent Framework — bridges Teams messages to goose serve",
  "main": "src/bot.js",
  "scripts": {
    "start": "node src/bot.js",
    "test": "jest --testPathPattern='src/bot\\.test\\.js' --forceExit"
  },
  "dependencies": {
    "botbuilder": "^4.22.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
